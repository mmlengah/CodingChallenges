#pragma once

/*
	Create a definition for the function isPositive
	The function should return whether a number is positive (true) or negative (false)
	Zero is not a positive number
*/

bool isPositive(int a) {
	return (a > 0);
}